# Repository Configuration
# Set the repository root directory (optional)
# Default: one level above the curadoria/ directory
REPO_ROOT=

# Stack files directory (optional)
# Default: same directory as the script
STACK_DIR=

# Destination directory for copied files (optional)
# Default: 'github-base' relative to REPO_ROOT
DEST_DIR=github-base
